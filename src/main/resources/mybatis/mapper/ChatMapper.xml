<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTO Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper	namespace="com.ezen.springmvc.domain.chat.mapper.ChatMapper">
	<!-- 채팅방 생성 -->
	<insert id="createChat" parameterType="com.ezen.springmvc.domain.chat.dto.ChatDto" >
		INSERT INTO chat_room(room_id, created_at, sender_id, receiver_id)
		VALUES(#{roomId}, #{createdAt}, #{sender}, #{receiver})
	</insert>

	<!-- 채팅방 전체목록 조회 -->
		<select id="findChatAll" resultType="ChatDto">
			SELECT room_id, created_at, sender_id, receiver_id
			FROM chat_room
			ORDER BY created_at DESC
		</select>

	<!-- 회원 닉네임으로 Like 검색  -->
	<select id="findChatByNick" parameterType="string" resultType="MessageDto">
		SELECT room_id, created_at, sender_id, receiver_id
		FROM message
		WHERE sender_id LIKE '%' || #{value} || '%'
	</select>


<!--	&lt;!&ndash; 회원 닉네임으로 Like 검색 &ndash;&gt;-->
<!--	<select id="findByNameLike" parameterType="string" resultType="MemberDto">-->
<!--		SELECT member_id "id", name, email, TO_CHAR(regdate, 'yyyy-MM-DD HH24:MI:SS') regdate-->
<!--		FROM member-->
<!--		WHERE name LIKE '%' || #{value} || '%'-->
<!--	</select>-->


<!--	&lt;!&ndash; 검색 타입별 회원 검색 &ndash;&gt;-->
<!--	<select id="findBySearchType" parameterType="string" resultType="MemberDto">-->
<!--		SELECT member_id "id", name, email, TO_CHAR(regdate, 'yyyy-MM-DD HH24:MI:SS') regdate-->
<!--		FROM member-->
<!--		<where>-->
<!--			<if test="type != null and type == 'id'">-->
<!--				and member_id = #{value}-->
<!--			</if>-->
<!--			<if test="type != null and type == 'name'">-->
<!--				and name LIKE '%' || #{value} || '%'-->
<!--			</if>-->
<!--		</where>-->
<!--	</select>-->

<!--	&lt;!&ndash; 회원 아이디 또는 이름 회원 통합 검색 &ndash;&gt;-->
<!--	<select id="findBySearchAll" parameterType="string"	resultType="MemberDto">-->
<!--		SELECT member_id "id", name, email, TO_CHAR(regdate, 'yyyy-MM-DD HH24:MI:SS') regdate-->
<!--		FROM member-->
<!--		WHERE member_id = #{value} OR name LIKE '%' || #{value} || '%'-->
<!--	</select>-->

<!--	&lt;!&ndash; 옵션 선택별 회원 통합 검색 &ndash;&gt;-->
<!--	<select id="findBySearchAllOption" parameterType="MemberSearchCondition" resultType="MemberDto">-->
<!--		SELECT member_id "id", name, email, TO_CHAR(regdate, 'yyyy-MM-DD HH24:MI:SS') regdate-->
<!--		FROM member-->
<!--		<where>-->
<!--			<if test="memberId != null and memberId != ''">-->
<!--				or member_id = #{memberId}-->
<!--			</if>-->
<!--			<if test="name != null and name != ''">-->
<!--				or name LIKE '%' || #{name} || '%'-->
<!--			</if>-->
<!--			<if test="email != null and email != ''">-->
<!--				or email = #{email}-->
<!--			</if>-->
<!--		</where>-->
<!--	</select>-->
</mapper>

