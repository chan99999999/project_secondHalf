<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTO Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ezen.springmvc.domain.category.mapper.CategoryMapper">
    <!-- 카테고리 목록 조회 -->
    <select id="findByCategoryAll" resultType="CategoryDto">
        SELECT category_id, category_name, admin_id
        FROM category
    </select>

</mapper>

