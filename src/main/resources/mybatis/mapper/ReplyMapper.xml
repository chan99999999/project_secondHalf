<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTO Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ezen.springmvc.domain.meetArticle.mapper.ReplyMapper">
    <!-- 댓글 등록 -->
    <insert id="createReply" parameterType="ReplyDto">
        INSERT INTO reply (reply_id,
                           m_article_id,
                           content,
                           regdate)
        VALUES (reply_id_seq.NEXTVAL,
                #{meetArticleId},
                #{content},
                SYSDATE)
    </insert>
    <!-- 댓글 목록 -->
    <select id="findByReplyAll" parameterType="int" resultType="ReplyDto">
        SELECT reply_id,
               m_article_id,
               content
        FROM reply
        WHERE m_article_id = #{meetArticleId}
        ORDER BY reply_id DESC
    </select>
    <!-- 댓글 수정 -->
    <update id="updateReply" parameterType="ReplyDto">
        UPDATE reply
        SET content = #{content}
        WHERE reply_id = #{replyId}
    </update>
    <!-- 댓글 삭제 -->
    <delete id="deleteReply" parameterType="ReplyDto">
        DELETE FROM reply
        WHERE reply_id = #{replyId}
    </delete>
    <!-- 댓글 갯수 -->
    <update id="replyCount" parameterType="int">
        SELECT COUNT(*)
        FROM reply
        WHERE m_article_id = #{mArticleId}
    </update>
</mapper>